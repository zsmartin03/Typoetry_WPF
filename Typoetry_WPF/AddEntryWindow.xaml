<Window x:Class="Typoetry_WPF.AddEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Entry" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Name="txtTitle" Grid.Row="0" PlaceholderText="Title" Margin="0,0,0,10" />
        <TextBox Name="txtAuthor" Grid.Row="1" PlaceholderText="Author" Margin="0,0,0,10" />
        <TextBox Name="txtText" Grid.Row="2" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" />
        <StackPanel Orientation="Horizontal" Grid.Row="3">
            <Button Content="Save" Click="btnSave_Click" />
            <Button Content="Cancel" Click="btnCancel_Click" Margin="10,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
